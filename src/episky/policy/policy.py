"""Operator-owned policy.

Owner: RFC-0008 §7; RFC-0001 §8.2.
Responsibility: Operator-owned policy, loaded default-deny.
Forbidden responsibility: a blocked action is only ever
    unblocked by an audited override (I-12, P10).
"""
